import Grid from '@mui/material/Unstable_Grid2';
import Typography from '@mui/material/Typography';

const Page = () => {
  return (
    <Grid container spacing={3}>
      <Grid>
        <Typography variant='h3'>Excel Upload</Typography>
      </Grid>
    </Grid>
  );
};

export default Page;
