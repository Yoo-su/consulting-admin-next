'use client';

import Stack from '@mui/material/Stack';

const AccountSettingBox = () => {
  return <Stack direction={'row'} justifyContent={'center'} alignItems={'center'} spacing={4}></Stack>;
};

export default AccountSettingBox;
