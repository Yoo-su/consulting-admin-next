import { APP_PAGE_KEYS } from './app-page-keys';
import { FEATURE_KEYS } from './feature-keys';
import { SHARED_KEYS } from './shared-keys';

export const QUERY_KEYS = { ...APP_PAGE_KEYS, ...SHARED_KEYS, ...FEATURE_KEYS };
