import { VersionServer } from '../models';

export const VERSION_SERVER: VersionServer[] = [
  { value: 'testDb', label: '테스트' },
  { value: 'realDb', label: '리얼' },
];

export const MIN_LIST_LENGTH = 6;
